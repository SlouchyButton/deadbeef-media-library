{
    "tasks": [
        {
            "label": "debug",
            "type": "shell",
            "command": "meson setup build && meson configure build --buildtype debug && cd build && ninja && cp ${workspaceFolder}/build/src/libddb_misc_media_library.so ~/.local/lib/deadbeef/ddb_misc_media_library.so",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "focus": true,
                "panel": "dedicated",
                "showReuseMessage": false,
                "clear": true
            }
        }
    ],
    "version": "2.0.0"
}